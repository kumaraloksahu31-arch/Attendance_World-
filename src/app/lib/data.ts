
import type { User, AttendanceSheet, AttendanceRecord } from './types';

// This file is kept for type definitions, but the mock data is no longer used
// as data is now fetched directly from Firestore.

export const users: User[] = [];

export const sheets: AttendanceSheet[] = [];

export const attendanceRecords: AttendanceRecord[] = [];
